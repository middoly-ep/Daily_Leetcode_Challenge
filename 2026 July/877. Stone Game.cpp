#include <vector>

using namespace std;

class Solution {
public:
    bool stoneGame(vector<int>& piles) {
        return true;
    }
};
//Given that there is always an even number of piles and odd number of stones, Alice can always choose all even indices or all odd indices, so Alice always win by choosing the half with more stone
